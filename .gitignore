.env
.env.env

# Chứa thông tin hạ tầng thật (IP, domain, lỗ hổng chưa vá) - không đăng công khai
RELEASE_NOTE.md

# Terraform: cache/provider và state cục bộ, ở bất kỳ thư mục nào trong repo
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan

# SSH private key tự sinh bởi Terraform (aws/terraform)
*.pem

# Biến có thể chứa giá trị nhạy cảm, ở bất kỳ thư mục nào trong repo
*.tfvars
*.tfvars.json
